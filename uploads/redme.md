# Don't delete this folder

This folder is used by the `@vercel/next.js` package to store the cache of the Next.js application. It is not necessary to add this folder to your `.gitignore` file.

If you want to delete this folder, you can do so by running the following command:

```bash

```
